version: '3.8'

services:
  jusartificial:
    build: .
    ports:
      - "3001:3001"
    environment:
      - API_KEY=${API_KEY}
      - PORT=3001
    volumes:
      - ./data:/app/data # Persistência simulada se necessário
    restart: always
